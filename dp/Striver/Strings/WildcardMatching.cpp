#include <iostream>
#include <string>
#include <vector>
#include<algorithm>
using namespace std;
class Solution {
public:
    bool bruteForce(string s, string p, int i, int j) {
        
    }
    bool isMatch(string s, string p) {
        
    }
};

int main() {
    Solution s1;
    string s = "adceb";
    string t = "*a*b";
    cout << s1.isMatch(s,t);
}